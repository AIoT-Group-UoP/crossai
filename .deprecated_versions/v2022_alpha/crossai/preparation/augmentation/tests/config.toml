# [crop]
# n_times = 1
# size=1000

[time_wrap]
n_times = 10
prob = 0.5

[add_noise]
n_times = 10
prob = 0.5

[quantize]
n_times = 10
prob = 0.5
n_levels = [10,30]

# [drift]
# n_times = 1
# prob = 0.5
# max_drift=0.5 
# n_drift_points=3
# kind='additive'
# per_channel=true
# normalize=true